{
  "name": "auth",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/website/auth/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:website"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/website/auth"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
